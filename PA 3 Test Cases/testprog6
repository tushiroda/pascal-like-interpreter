program circle;
var
	{Illegal operand types for an arithmatic operator}
	r, a, p, b : real; 
i, j : integer;	
	bool : boolean := true;
begin
	r := 8;
	p := 0;
	a := 0;
	j := -1;
	if r > 5 then
	  a := r * j * bool;
	
	{Display the results}
	writeln ( 'The output results are ' , r, p, b);
end.
