# Simple Pascal-like language tokenizer, parser, and interpreter

This is a simple pascal-like language interpreter written in C++. prog3.cpp` takes an input file as an argument and parses its contents. It will print any errors it comes across and exit if the program would fail to compile.

